[project]
name = "sdlc-multiagent-automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shashankwv", email = "41374464+shashankwv@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "langchain>=1.4.0",
    "langchain-google-genai>=4.4.0",
    "langchain-groq>=1.1.3",
    "langchain-openai>=1.6.2",
    "langgraph>=1.2.11",
    "pandas>=3.0.5",
    "psycopg2-binary>=2.9.12",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
    "sqlglot>=30.18.0",
    "streamlit>=1.63.0",
    "uvicorn>=0.52.4",
]

# [project.scripts]
# sdlc-multiagent-automation = "sdlc_multiagent_automation:main"

# [build-system]
# requires = ["uv_build>=0.12.11,<0.13.0"]
# build-backend = "uv_build"

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
